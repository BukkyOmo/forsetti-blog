language: node_js
node_js:
  - "stable"
cache:
  directories:
    - "node_modules"
services:
    - "postgres"
before_script:
  - "psql -c 'create database travis;' -U postgres"
  - npm run pretest
script:
  - npm ci
  - npm test
